# Dependencies
node_modules/

# Build output
dist/
dist-ssr/

# Env files
.env
.env.*
!.env.example

# Editor / OS
.DS_Store
*.local

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# This process's own scratch workspace and the archived design reference —
# never commit either into this repository's history (ruling R8).
.reference/
.superpowers/

# Per-session git worktrees (root CLAUDE.md: one worktree per parallel session)
.claude/worktrees/
.agents/
