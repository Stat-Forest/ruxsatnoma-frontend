/** Rendered by RequireAuth when the signed-in user lacks the required permission. */
export function Forbidden() {
  return <div data-testid="forbidden">Sizda ushbu sahifaga kirish huquqi yo'q.</div>;
}
