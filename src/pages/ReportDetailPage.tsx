/**
 * J2 — placeholder for the report detail page (`GET /reports/{report_id}`
 * and its whole lifecycle). Task 4 (nav + route wiring) creates this stub so
 * `routes.tsx`'s `reports/:id` route and `npm run typecheck` both pass
 * before the real screen exists; task 7 replaces this file's whole content
 * with the composed page (`ReportDataPanel` + `ReportLifecyclePanel` +
 * `ReportExportPanel`), the same placeholder-then-replace shape
 * `src/pages/placeholders.tsx`'s own header comment documents for every
 * other stage-6 screen.
 */
export function ReportDetailPage() {
  return <div className="p-6 text-sm text-[#5A646D] font-sans">Hisobot sahifasi qurilmoqda…</div>;
}
