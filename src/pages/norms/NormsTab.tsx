/**
 * Norms — stub for task 6 (`norms.norms.*` copy). A bare, no-props component
 * on purpose — see `NormsPage.tsx` for why it stays mounted while another
 * tab is active.
 */
export function NormsTab() {
  return (
    <div
      data-testid="norms-tab-norms"
      className="rounded-2xl border border-dashed border-[#E4E7EA] p-8 text-center text-sm text-[#5A646D]"
    >
      Norms screen — task 6.
    </div>
  );
}
