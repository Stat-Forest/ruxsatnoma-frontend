/**
 * Tariffs — stub for task 5 (`norms.tariffs.*` copy, reuses task 4's dialog
 * components per the master plan). A bare, no-props component on purpose —
 * see `NormsPage.tsx` for why it stays mounted while another tab is active.
 */
export function TariffsTab() {
  return (
    <div
      data-testid="norms-tab-tariffs"
      className="rounded-2xl border border-dashed border-[#E4E7EA] p-8 text-center text-sm text-[#5A646D]"
    >
      Tariffs screen — task 5.
    </div>
  );
}
